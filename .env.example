# Server Config
PORT=8000
HOST=0.0.0.0

# Gemini API Key (Required for AI analysis, fallbacks to mock if empty)
GEMINI_API_KEY=your_gemini_api_key_here

# Database Config (Optional local fallback is used if connection fails)
MONGODB_URI=mongodb://localhost:27017
DB_NAME=resume_analyzer
